{
  "name": "jacsal-sync-gis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy"
  }
}
